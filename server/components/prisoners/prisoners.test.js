const request = require('supertest');
const server = require('./index');
const db = require('./prisonersModel');

describe('Prisoners', () => {
    describe('Model', () => {

    });

    describe('Router', () => {

    });
});