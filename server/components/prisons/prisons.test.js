const request = require('supertest');
const server = require('./index');
const db = require('./prisonsModel');

describe('Prisons', () => {
    describe('Model', () => {
        
    });

    describe('Router', () => {
        
    });
});